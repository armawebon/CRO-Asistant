# Secretos: la clave del modelo vive aquí y nunca debe versionarse
.env
.env.*
!.env.example

# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp

# Dependencias / artefactos (la app no usa build, pero evita ruido si se añade tooling)
node_modules/
dist/
*.log
